# Example for vue-timetable

This project show how to use the `vue-timetable` npm package.

## Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
npm run dev
```
